{
    "title": "TikTok Trend Hunter Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "category": {
            "title": "Product Category",
            "type": "string",
            "description": "The TikTok Shop category to analyze (e.g., 'Kitchen Gadgets', 'Beauty', 'Electronics')",
            "editor": "textfield",
            "default": "Kitchen Gadgets"
        },
        "maxProducts": {
            "title": "Maximum Products",
            "type": "integer",
            "description": "Maximum number of products to analyze (1-50)",
            "default": 10,
            "minimum": 1,
            "maximum": 50
        },
        "aiProvider": {
            "title": "AI Provider",
            "type": "string",
            "description": "Choose the AI provider for analysis",
            "editor": "select",
            "default": "openrouter",
            "enum": ["openrouter", "anthropic", "openai"],
            "enumTitles": ["OpenRouter (Free models)", "Anthropic (Claude)", "OpenAI (GPT-4)"]
        },
        "openrouterApiKey": {
            "title": "OpenRouter API Key",
            "type": "string",
            "description": "Your OpenRouter API key (get free at openrouter.ai)",
            "editor": "textfield",
            "isSecret": true
        },
        "openrouterModel": {
            "title": "OpenRouter Model",
            "type": "string",
            "description": "Model to use (default: nvidia/nemotron-nano-9b-v2:free)",
            "editor": "textfield",
            "default": "nvidia/nemotron-nano-9b-v2:free"
        },
        "anthropicApiKey": {
            "title": "Anthropic API Key",
            "type": "string",
            "description": "Your Anthropic API key (required if using Anthropic)",
            "editor": "textfield",
            "isSecret": true
        },
        "openaiApiKey": {
            "title": "OpenAI API Key",
            "type": "string",
            "description": "Your OpenAI API key (required if using OpenAI)",
            "editor": "textfield",
            "isSecret": true
        },
        "includeReviewAnalysis": {
            "title": "Include Review Analysis",
            "type": "boolean",
            "description": "Analyze product reviews for sentiment and emotional triggers",
            "default": true
        },
        "minSalesCount": {
            "title": "Minimum Sales Count",
            "type": "integer",
            "description": "Only analyze products with at least this many sales",
            "default": 100,
            "minimum": 0
        }
    },
    "required": ["category", "maxProducts"]
}
